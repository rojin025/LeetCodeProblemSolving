function canAttendMeetings(intervals: number[][]): boolean {
  let res = false;

  return res;
}

console.log("Running CanAttend Meetings.");

console.log(
  canAttendMeetings([
    [0, 30],
    [5, 10],
    [15, 20],
  ])
);

console.log(
  canAttendMeetings([
    [0, 3],
    [5, 10],
  ])
);
